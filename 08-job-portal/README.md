# Job Portal System

MERN role-based job portal for candidates, employers, and admins. Includes job posting workflow, approval states, applications, status tracking, and resume URL support.

## Run

```powershell
cd backend; npm install; Copy-Item .env.example .env; npm run dev
cd ../frontend; npm install; npm run dev
```
