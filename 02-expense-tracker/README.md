# Expense Tracker

MERN expense tracker with JWT authentication, protected transactions, income/expense categories, and summary totals.

## Run

```powershell
cd backend; npm install; Copy-Item .env.example .env; npm run dev
cd ../frontend; npm install; npm run dev
```
