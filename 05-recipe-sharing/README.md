# Recipe Sharing Platform

MERN recipe sharing platform with authenticated recipe CRUD, category filtering, ingredients, preparation details, and image URL metadata.

## Run

```powershell
cd backend; npm install; Copy-Item .env.example .env; npm run dev
cd ../frontend; npm install; npm run dev
```
