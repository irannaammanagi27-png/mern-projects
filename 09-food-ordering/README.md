# Online Food Ordering

MERN food ordering app with menu browsing, local cart state, checkout, order history, and delivery status tracking.

## Run

```powershell
cd backend; npm install; Copy-Item .env.example .env; npm run dev
cd ../frontend; npm install; npm run dev
```
