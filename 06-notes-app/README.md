# Notes App

Private MERN notes app with JWT authentication, protected CRUD endpoints, timestamps, and search-ready note data.

## Run

```powershell
cd backend; npm install; Copy-Item .env.example .env; npm run dev
cd ../frontend; npm install; npm run dev
```
