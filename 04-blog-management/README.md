# Blog Management System

MERN blog platform with registration, login, JWT-protected authoring, post CRUD, publishing state, and public post browsing.

## Run

```powershell
cd backend; npm install; Copy-Item .env.example .env; npm run dev
cd ../frontend; npm install; npm run dev
```
