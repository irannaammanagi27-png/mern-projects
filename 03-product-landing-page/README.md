# Product Landing Page

Responsive React product landing page with an Express content API. Product highlights, pricing, testimonials, and contact capture are separated from the UI so the page can grow into a full product site.

## Run

```powershell
cd backend; npm install; npm run dev
cd ../frontend; npm install; npm run dev
```
